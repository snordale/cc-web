import { Box, Typography } from "@mui/material"

export const Landing: React.FC<{}> = ({ }) => {
	return (
		<Box>
			<Typography>
				Weekly playlist for the people who know what's worth it.
			</Typography>
		</Box>
	)
}