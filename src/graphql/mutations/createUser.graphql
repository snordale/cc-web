mutation createUser(
	$email: String!,
	$username: String!,
	$password: String!,
	$token: String!
) {
	createUser(data: {
		email: $email,
		username: $username,
		passwordHash: $password
		token: $token
	}) {
		user {
			...PrivateUser
		}
		errors {
			field
			message
		}
	}
}