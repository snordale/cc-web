mutation login($emailOrUsername: String!, $password: String!) {
	login(
		data: { emailOrUsername: $emailOrUsername, passwordHash: $password }
	) {
		user {
			...PrivateUser
		}
		errors {
			field
			message
		}
	}
}
